{
  "name": "tiny-canvas-monorepo",
  "private": true,
  "version": "0.8.0",
  "type": "module",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=demo",
    "build": "npm run build --workspace=packages/tiny-canvas",
    "build:demo": "npm run build --workspace=demo"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8"
  }
}
