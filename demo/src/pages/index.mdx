import { Block, Canvas, Frame, Link, Mark, Note } from '@dfosco/tiny-canvas'
import '@dfosco/tiny-canvas/style.css'
import { Button } from '@primer/react'
import { ThemeToggle } from '../components/ThemeToggle.jsx'
import { StateToggle } from '../components/StateToggle.jsx'

export const isEmbeddedView =
  typeof window !== 'undefined' &&
  new URLSearchParams(window.location.search).has('embedView')

<Canvas dotted={true} grid={false} resettable>
  <Block x={36} y={36}>
    <ThemeToggle />
  </Block>
  <Block x={396} y={180}>
    <Button size="large" variant="primary">Save</Button>
    <Button size="large" variant="secondary">Edit</Button>
    <Button size="large" variant="danger">Delete</Button>
  </Block>
  <Block x={180} y={360}>
    <StateToggle />
  </Block>
  <Note key="release-note" x={760} y={72} color="yellow">
    {'## Sticky note\nDrag and resize me. Changes persist.'}
  </Note>
  <Mark key="markdown-example" x={760} y={300} width={420}>
    {'### Markdown\n\n- Safe rendering\n- Draggable\n- Resizable'}
  </Mark>
  <Link
    key="tiny-canvas-link"
    url="https://github.com/dfosco/tiny-canvas"
    title="Tiny Canvas on GitHub"
    displayUrl="github.com/dfosco/tiny-canvas"
    x={760}
    y={500}
  />
  {!isEmbeddedView && (
    <Frame
      key="tiny-canvas-preview"
      route="/tiny-canvas/?urlstate=frame-demo#/"
      title="Tiny Canvas preview"
      x={36}
      y={520}
      width={704}
      height={480}
    />
  )}
</Canvas>
