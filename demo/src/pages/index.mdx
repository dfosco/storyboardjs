import { Block, Canvas, Frame } from '@dfosco/tiny-canvas'
import '@dfosco/tiny-canvas/style.css'
import { Button } from '@primer/react'
import { ThemeToggle } from '../components/ThemeToggle.jsx'
import { StateToggle } from '../components/StateToggle.jsx'

export const isEmbeddedView =
  typeof window !== 'undefined' &&
  new URLSearchParams(window.location.search).has('embedView')

<Canvas dotted={true} grid={false} resettable>
  <Block x={36} y={36}>
    <ThemeToggle />
  </Block>
  <Block x={396} y={180}>
    <Button size="large" variant="primary">Save</Button>
    <Button size="large" variant="secondary">Edit</Button>
    <Button size="large" variant="danger">Delete</Button>
  </Block>
  <Block x={180} y={360}>
    <StateToggle />
  </Block>
  {!isEmbeddedView && (
    <Frame
      key="tiny-canvas-preview"
      route="/tiny-canvas/?urlstate=frame-demo#/"
      title="Tiny Canvas preview"
      x={36}
      y={520}
      width={704}
      height={480}
    />
  )}
</Canvas>
